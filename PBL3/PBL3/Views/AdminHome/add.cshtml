@model PBL3.Models.TradeMark
@{
    ViewBag.Title = "add";
}

<h2>add</h2>

@using (Html.BeginForm("add", "AdminHome", FormMethod.Post))
{
    @Html.LabelFor(model => model.name);
    @Html.TextBoxFor(model => model.name);

    <input type="submit" value="Add Trademark" />
}